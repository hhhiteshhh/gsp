import React from "react";

const styles = {
  container: {
    display: 'flex',
    height: '100vh',
    width: '100wh',
    justifyContent: 'center',
    alignItems: 'center'
  }
}

const Login = () => (
  <div style={styles.container}>
    Loader..
  </div>
);

export default Login;